# OTT Update Bot — Python dependencies
#
# Pin major versions for reproducibility on Railway.
# Run: pip install -r requirements.txt

# HTTP client — used by TMDB and Telegram API callers
requests>=2.31,<3.0

# MongoDB driver
pymongo>=4.6,<5.0

# Environment variable loading (.env file support)
python-dotenv>=1.0,<2.0

# Timezone-aware date handling (Asia/Kolkata)
pytz>=2024.1
